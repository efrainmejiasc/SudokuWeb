USE [DB_A3EF3F_SUDOKU]
GO

SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE PROCEDURE [dbo].[Sp_ActivarInactivarDesactivarCliente]
(
  @MAIL VARCHAR(50),
  @ESTADO VARCHAR (50) 
)
AS
BEGIN

SET NOCOUNT ON;

UPDATE CLIENTE SET ESTADO = @ESTADO WHERE MAIL = @MAIL;

END 

